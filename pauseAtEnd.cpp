// Another small function as another option to pause at the end of a program
// and keep the console window open

// This will simply be used just prior to return 0; in the end of main()

void pause() {
	std::cin.sync();
	std::cin.get();
}